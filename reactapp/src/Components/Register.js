import React from "react";

const Register = () => {
  return <div></div>/*<div>Register
    <div
            style={{
              display: "flex",
              flexDirection: "column",
              justifyContent: "center",
              alignItems: "center",
            }}
          >
            Password
            <TextField
              id="password"
              label="Password"
              type="password"
              value={password}
              onChange={handlePasswordChange}
              error={passwordError}
              helperText={passwordError ? "Incorrect password!" : ""}
            />
          </div>
  </div>;*/
};

export default Register;
